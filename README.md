# rc 0.1.0

## Riff Compiler

Generates executable files from Riff code based on an interpreter that is packaged with your code.

Usage:

Run `rc` if downloaded from releases, or `./rc` if within the source code for help info.

Documentation:

Check `syntax.txt` and the examples.
